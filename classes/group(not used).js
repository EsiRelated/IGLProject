/*module.exports = class Group{
    constructor(id,courses){
        this.id = id;
        this.courses = courses; // map<moduleName,[Course]>
    }
    getSubjectCourses(subjectName){
            let couple =  courses.getCouple(subjectName);
            if (couple == 'undefined')
                throw new Error('subject not found');
            else 
                return couple.getValue();
    }
}*/
